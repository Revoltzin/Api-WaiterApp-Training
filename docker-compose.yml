services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: rachadinha
      POSTGRES_PASSWORD: rachadinha
      POSTGRES_DB: rachadinha
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
